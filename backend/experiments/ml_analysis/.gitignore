# Reproducible caches, not results. Everything listed here is regenerated by
# run_all.py from the benchmark parquet and the published RNA-FM weights.
#
# NOTE: a trailing comment on a pattern line is part of the pattern, not a
# comment. Keep these on their own lines.

# ~6 MB of trained ranker checkpoints, keyed by a config hash.
results/models/

# ~90 MB, about 25 minutes of CPU to rebuild.
results/rnafm_embeddings.npz

results/*.log
